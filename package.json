{
  "name": "quantum-dither-license-vercel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check api/activate.js && node --check api/sync-license.js && node --check api/health.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
