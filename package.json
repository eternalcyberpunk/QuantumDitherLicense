{
  "name": "eternal-cyberia-license-vercel",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check api/activate.js && node --check api/sync-license.js && node --check api/health.js && node --check api/installer-resolve.js && node --check api/installer-authorize.js && node --check lib/catalog.js"
  },
  "dependencies": {
    "@vercel/blob": "^2.3.0",
    "pg": "^8.13.1"
  }
}
